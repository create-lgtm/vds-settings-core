1. Enter your VDS server data: vds_config.txt
2. Next, run main.exe, which will connect to plink.exe
3. If plink.exe isn't installed, the program will install it automatically.
4. The server will configure and reconfigure ports, and then restart.
